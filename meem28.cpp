#include<bits/stdc++.h>
int main ()
{
    int p,q;
    scanf("%d%d",&p,&q);
    printf("enter the numbers p and q :");


    if(p>q) {
        printf("%d is the largest number",p);

    }

        else { printf("%d is the largest number",q);
}
    return 0;
}
